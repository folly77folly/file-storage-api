setup:
  addons:
    - plan: mongolab:sandbox
      as: DATABASE
build:
  docker:
    web: Dockerfile
run:
  web: bundle exec puma -C config/puma.rb
  worker:
    command:
      - python manage.py runserver
    image: web