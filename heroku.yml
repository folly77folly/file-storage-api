# build:
#   docker:
#     web: Dockerfile
# run:
#   web: bundle exec puma -C config/puma.rb

setup:
    addons:
      - plan: mongolab:sandbox
        as: DATABASE
  build:
    docker:
      web: Dockerfile
  run:
    web: bundle exec puma -C config/puma.rb
    worker: python manage.py runserver